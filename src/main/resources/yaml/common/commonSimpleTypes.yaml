components:
  schemas:
    Alpha2LangCodeType:
      type: string
      maxLength: 2
      description: '2 letter alphabetic code of language (ISO 639-1:2002)'

    ProductCategoryIdType:
      type: string
      description: 'technický názov kategórie produktu'

    ProductIdType:
      type: string
      description: 'ID produktu'

    TechnicalProductIdType:
      type: string
      description: 'technické ID produktu'

    Alpha3CurrencyCodeType:
      type: string
      description: '3 letter alhpabetic code of currency (ISO 4217)'

    ISINNumberType:
      type: string
      description: 'Základný identifikátor emisií cenných papierov (CP) a finančných nástrojov'

    ProductAttrTechnicalNameType:
      type: string
      description: 'Technical identificator of product attribute'

    FilterDateFromType:
      type: string
      format: date
      description: 'Date from'

    FilterDateToType:
      type: string
      format: date
      description: 'Date to'

    FilterSpecificDateType:
      type: string
      format: date
      description: 'Specific date'

    Alpha2CountryCodeType:
      type: string
      description: '2 letter alphabetic code of country (ISO 3166)'

    FrameContractIdType:
      type: string
      description: 'Unique identifier of frame contract'
